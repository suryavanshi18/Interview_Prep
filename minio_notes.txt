version: '3.8'

services:
  minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z-cpuv1
    container_name: minio
    volumes:
      - /root/minio/data:/data
      - /home/minio-setup/setup-minio/scripts/minio_25:/root/scripts/
      - /home/certificate/minio_25:/root/.minio/certs
      - /home/certificate/minio_25/public.crt:/etc/ssl/certs/ca-certificates.crt
    ports:
      - "9000:9000"
      - "9090:9090"
    environment:
      - MINIO_ROOT_USER=FIQ0
      - MINIO_ROOT_PASSWORD=W86uNTp
      - MINIO_BROWSER_REDIRECT_URL=https://xxx.yy.uu.jjj:9090
      - MINIO_SKEW_TIME=86400
    command: server /data --console-address ":9090"
    #restart: always


9000 is the api endpoint and 9090 is the webconsole

############## Generate certificate ########################


curl -LO https://github.com/minio/certgen/releases/latest/download/certgen-linux-amd64

chmod +x certgen-linux-amd64

# Generate certs
./certgen-linux-amd64 -host "xxx.yy.uu.jjj,localhost"